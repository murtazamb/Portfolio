document.addEventListener('DOMContentLoaded',()=>{const s=document.querySelector('.showcase');if(!s)return;const slides=[...s.querySelectorAll('.slide')],c=s.querySelector('.slider-controls'),dots=[...s.querySelectorAll('[data-slide]')],status=s.querySelector('#slide-status');c.hidden=false;let i=0;const show=n=>{i=(n+slides.length)%slides.length;slides.forEach((x,j)=>x.hidden=j!==i);dots.forEach((d,j)=>d.setAttribute('aria-pressed',j===i));if(status)status.textContent=`0${i+1} / 0${slides.length}`};dots.forEach(d=>d.onclick=()=>show(+d.dataset.slide));s.querySelectorAll('[data-direction]').forEach(b=>b.onclick=()=>show(i+(+b.dataset.direction)));s.onkeydown=e=>{if(e.key==='ArrowRight')show(i+1);if(e.key==='ArrowLeft')show(i-1)};let t=setInterval(()=>show(i+1),7000);s.onmouseenter=()=>clearInterval(t);s.onmouseleave=()=>t=setInterval(()=>show(i+1),7000)});
